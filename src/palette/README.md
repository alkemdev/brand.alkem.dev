# Design Tokens

`tokens.json` is the single source of truth for all brand values:
colors, typography scale, spacing, radii, and shadows.

Run `just tokens` to generate CSS custom properties and SCSS variables
in `out/stylesheets/`.
